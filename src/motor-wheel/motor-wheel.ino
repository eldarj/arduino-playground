int switchState = 0;
bool turnedOn = false;

void setup() {
  // pinMode(9, OUTPUT);
  // pinMode(2, INPUT);
  // pinMode(10, OUTPUT);

  // Serial.begin(9600);
}

void loop() {
  // switchState = digitalRead(2);

  // if (switchState == HIGH) {
  //   turnedOn = !turnedOn;
  // }


  // digitalWrite(9, turnedOn ? HIGH : LOW);
  // digitalWrite(10, turnedOn ? HIGH : LOW);

  // Serial.print("Status is: ");
  // Serial.println(switchState);
  // delay(100);
}
